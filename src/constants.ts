/**
 * Default delay between animated QR frames. The CatCard camera is relatively slow,
 * so frames must stay on screen long enough to be decoded reliably.
 */
export const DEFAULT_FRAME_INTERVAL_MS = 600;
